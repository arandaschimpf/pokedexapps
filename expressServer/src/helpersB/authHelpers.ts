export const validateLogin = (username: string, password: string): boolean => {
    // Aquí iría la lógica para validar las credenciales de login
    return true;
  };
  
  export const validateSignup = (username: string, password: string): boolean => {
    // Aquí iría la lógica para validar las credenciales de signup
    return true;
  };
  